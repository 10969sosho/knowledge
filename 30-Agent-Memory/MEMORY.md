# CENTRAL MEMORY STATE (HERMES ⇄ LAPTOP)

- **Repo Knowledge SSOT**: `https://github.com/10969sosho/knowledge.git`
- **Server Alurelab**: `emerald.hidden-server.net:31988`, user `alurelab`.
- **Server KBKB**: `157.15.77.18:31988`, user `kbkbid`, domain `kbkb.id`.
- **OpenCode Model Policy**:
  - **Default / Basic**: `openrouter/dots-studio/dots-3-note-preview:free` (OpenRouter Dots 3 Note Preview).
  - **Alternative / Mimo**: `opencode/mimo-v2.5-free` (MiMo V2.5 via native OpenCode / OpenZen) atau `openrouter/xiaomi/mimo-v2.5`.
  - **Paid / Fallback Lain**: `bandelbanget/claude-sonnet-4.5`, `bandelbanget/gpt-5.6-luna`, `bandelbanget/deepseek-v4-pro`.
- **cPanel Next.js Ports**: Port 3001 (furniture), 3002 (kawasaki). Jangan tabrak port, selalu cek `ps aux | grep next-server`.
- **Symlink Rule Next16 cPanel**: Copy `.next/static/.` ke `public/_next/` lalu symlink `ln -s . public/_next/static`.
- **Seeder/Migration**: Jangan rely pada `php -l`. Runtime verify seeder dengan driver MySQL/MariaDB compatible syntax.
- **ALURELAB Webhooks**: Xendit callback wajib memakai `XENDIT_WEBHOOK_TOKEN`; Biteship webhook memakai custom header/secret dan harus fail-closed jika belum dikonfigurasi.
- **ALURELAB Audit Batch**: Fallback transaksi, buyer phone IDOR, tenant header tanpa membership, mock payout/AWB, public settings leak, dan Redis/DB inventory divergence sudah diputus. Durable inventory reservation expiry/release sudah selesai. Voucher atomik dan Xendit disbursement/reconciliation belum selesai.
- **ALURELAB Hosting**: Production `app.alurelab.com` deployed/current, PM2 `alurelab-frontend` port 3040 online, Laravel scheduler cron aktif setiap menit, migration reservation applied, and runtime backup created at `/home/alurelab/app.alurelab.com.bak_20260916_1122`.
- **ALURELAB Xendit**: Production `XENDIT_WEBHOOK_TOKEN` updated and config cache rebuilt. Invalid token returns 401; configured token reaches payload validation (400 for empty payload). Token value is intentionally not stored in knowledge.
- **ALURELAB Xendit**: Development public key configured in production environment to match the existing development secret key. Backup created at `/home/alurelab/app.alurelab.com.bak_20260916_1122/backend.env.before_xendit_public_key`.
- **ALURELAB Xendit**: Valid signed test callbacks with unknown `external_id` now return 200 `ignored` instead of 404, allowing Dashboard Test and save without processing a fake order. Production backup: `/home/alurelab/app.alurelab.com.bak_20260916_xendit`.
- **ALURELAB Biteship**: Rates-only validation found production `BITESHIP_API_KEY` is still a dummy key, so real rates require the user to configure a Biteship API key. Webhook is not required for rate lookup. Store origin must be configured; hardcoded origin fallback was removed from the rates endpoint.
- **ALURELAB Biteship**: Live API key configured in production and config cache rebuilt. Rates test now reaches store validation; `kalmora` is missing `address_area_id`. Backup: `/home/alurelab/app.alurelab.com.bak_20260916_biteship_rates/backend.env.before_biteship_key`.
- **ALURELAB NextAuth CSRF Fix**: Node.js 20.x encodes `|` to `%7C` in Set-Cookie headers, breaking NextAuth v5 beta.32 CSRF token verification (`token|hash` format). Two server-side patches in `@auth/core` (persisted via `postinstall`): (1) `csrf-token.js`: decode `%7C` before splitting cookie value; (2) `index.js`: parse `ReadableStream` body for App Router compatibility. Also removed `auth()` middleware wrapper from `middleware.ts` to prevent double CSRF token generation. Credentials login flow uses `/api/auth/callback/credentials` (not `/signin/credentials`). Backup: `/home/alurelab/app.alurelab.com.bak_20260916_middleware/`.
- **PAYROLL Fix (2026-09-17)**: `AttendanceDailyController::daily()` - `Undefined variable $permits` fix by adding `Permit::where('permit_date', $dateStr)->get()` before employee loop. Domain: payroll.3putraperkasa.com, repo: ~/repositories/solution.
- **PAYROLL Revisions (2026-09-17)**: Production updated to commit `b409546`: izin menu removed from sidebar (routes retained for compatibility), loan report now uses current-month loans and payroll `pinjaman_deduction`, employee export print-to-PDF page added, work settings support multiple golongan via `work_setting_golongan`, and name search added to daily/detail attendance. Server backup: `/home/alurelab/repositories/solution.bak_20260917_revisi_menu`.
- **PAYROLL Test Seed (2026-09-17)**: Seeder `AugustTestingSeeder` committed as `a49e7cd` and run safely on production for August 2026 using existing active employees. Created 310 daily rows, 630 marked attendance logs, 1 permit, 8 loans, and 4 loan payments; rerun is idempotent. Payroll August records already existed and were paid, so they were not overwritten. Database backup: `/home/alurelab/repositories/payroll_before_seed_agustus_20260917_131522.sql`; source backup: `/home/alurelab/repositories/solution.bak_20260917_seed_agustus`.
- **PAYROLL Daily/Recap Deploy (2026-09-21)**: Production commit `9239906` fixes work-setting form 500 caused by unsafe optional day input, makes checklock ranges final (`06:00-09:00`, `11:00-12:29:59`, `12:30-13:30`, `15:30-20:00`) without fallback misclassification, adds daily anomaly/normal filter, allows `Set Draft`, `Set Lock`, and `Fix Semua & Simpan` without per-row FIX, and creates missing employee-day records while opening monthly recap so payroll has complete attendance data. Local tests: 43 passed / 124 assertions. Production backups: `/home/alurelab/repositories/solution.bak_20260921_daily_recap`, `/home/alurelab/repositories/solution.bak_20260921_work-setting-array`, `/home/alurelab/repositories/solution.bak_20260921_work-setting-final`. Remote settings views rendered successfully and cache rebuild completed. Existing 42 dummy rows with the old unordered-scan reason were normalized to `12:00/13:00` and backed up at `/home/alurelab/repositories/attendance_order_anomaly_before_20260921.json`; remaining count is 0.
- **PAYROLL Requirements Deploy (2026-09-22)**: Deployed attendance daily multi-izin/type controls, removed Process action from detailed attendance report, corrected detailed/summary report columns and calculations, corrected monthly loan previous balance to use loan payments, added `addition` permit type migration, and added `AttendanceDummySeeder`. Production migration `2026_09_22_000001_allow_addition_permit_type` ran successfully. Dummy verification: 10 active employees, July 310/310 daily fixed, 31/31 compilations fixed, 10 July payroll paid, August 310 daily with 0 anomalies and 1240 seeded scans, September 300 daily with 0 anomalies and 1200 seeded scans. Backups: `/home/alurelab/repositories/payroll_before_requirements_20260922.sql` and `/home/alurelab/repositories/solution.bak_20260922_requirements.tar.gz`. Login endpoint 200; unauthenticated attendance endpoint redirects 302 to login.
- **PAYROLL Permit Popup (2026-09-22)**: Daily attendance permit entry is now a per-employee popup showing existing permits and allowing multiple new permits without widening the table. Production source backup: `/home/alurelab/repositories/solution.bak_20260922_permit-popup.tar.gz`; cache rebuild passed; login remains HTTP 200 and unauthenticated daily remains HTTP 302 to login.
- **PAYROLL Permit Popup Fields (2026-09-22)**: Popup now separates `Tipe izin` (terlambat, pulang awal, tidak masuk) from `Keterangan izin` (potong gaji, tambah gaji, tidak potong). Nominal is required and stored for the first two; minutes is required for no deduction. Migration `2026_09_22_000002_add_nominal_to_permits_table` ran successfully. Production backup: `/home/alurelab/repositories/payroll_before_permit_fields_20260922.sql` and `/home/alurelab/repositories/solution.bak_20260922_permit-fields.tar.gz`; schema check confirms `permits.nominal` exists.
- **CMC_TRADER (2026-09-17)**: Build spec implemented at `/Users/10969sosho/PROJECTS/finance/PROJECT/PROJECT ANTIGRAVITY/CMC_TRADER`. Python 3.12 required for pinned dependencies; Python 3.14 fails on `pandas==2.1.4`. Fixed `BreakoutStrategy` roll_low initialization and `PortfolioTracker` SQLite schema initialization. Tests: 9 passed. Live mode not run.
- **ALURELAB Registration (2026-09-17)**: Onboarding no longer asks for optional first product or auto-logins. It creates the seller store, sends Laravel email verification, blocks unverified login, and verified login lands on `/dashboard`. Production needs an active `MAIL_*` configuration and `FRONTEND_URL` for the verification redirect.
- **ALURELAB SMTP (2026-09-18)**: Production backend SMTP configured with `mail.alurelab.com:465` using SSL, sender `hi@alurelab.com`, and config cache rebuilt. Laravel SMTP test sent successfully. Password intentionally not stored in knowledge. Backup: `/home/alurelab/app.alurelab.com/backend/.env.before_mail_20260918_000339`.
- **ALURELAB Registration Deploy (2026-09-18)**: Email-verification onboarding changes deployed to `/home/alurelab/app.alurelab.com`; frontend built and PM2 `alurelab-frontend` restarted on port 3040, Laravel config/cache rebuilt. Production source backup: `/home/alurelab/app.alurelab.com.bak_20260918_registration`.
- **ALURELAB Verification Link Fix (2026-09-18)**: Root `/email/verify/...` links were caught by Next.js storefront routing. Added API verification route plus frontend redirect and legacy-path signature validation. Existing verification URL tested successfully through `/login?verified=1`. Backup: `/home/alurelab/app.alurelab.com.bak_20260918_legacy_signature`.
- **ALURELAB Seller Dashboard Cleanup (2026-09-18)**: Deployed dashboard cleanup: unused header icons removed, bulk shipping hidden, starter display maps to Free when no paid plan exists, wilayah.id cascading address selectors added for seller/buyer/checkout, seller Biteship origin fields persisted, checkout rates loaded from Biteship, and per-store master category page with product category datalist added. Build and 12 PHPUnit tests pass. Backup: `/home/alurelab/app.alurelab.com.bak_20260918_dashboard_cleanup`.
- **ALURELAB wilayah.id CORS Fix (2026-09-18)**: Browser calls to wilayah.id failed because the API had no CORS header. Added same-origin Next.js proxy at `/api/wilayah/[...path]`, deployed and rebuilt frontend. Production `/api/wilayah/provinces` returns 200. Backup: `/home/alurelab/app.alurelab.com.bak_20260918_wilayah_cors`.
- **ALURELAB Buyer Template Cleanup (2026-09-18)**: Local buyer UI pass for L Kids: cleaner editorial copy and controls on home, product detail, cart, checkout, and account; removed hardcoded trust/promotional copy from defaults; Editorial value pillars now render from CMS only. Frontend production build passes. Not deployed yet. Backups are beside touched frontend files with suffix `_20260918_buyer-clean`.
- **ALURELAB Buyer Template Deploy (2026-09-18)**: Buyer cleanup deployed to production in commits `c98f0bf` and `8ea51af`. Production backup: `/home/alurelab/app.alurelab.com.bak_20260918_003626_buyer-clean`; failed build cache retained as `.next.bak_20260918_003643_buyer-build-failed`; wilayah proxy route backup created with suffix `_proxy-fix`. Frontend build passed, PM2 `alurelab-frontend` reloaded online, storefront buyer routes return 200, and `/api/wilayah/provinces.json` returns 200 with 38 provinces.
- **ALURELAB L Kids Navigation Simplification (2026-09-18)**: Removed rendered collections, lookbook, contact/security, and value-pillar blocks from Editorial home. About now shows only CMS title and description. Drawer/footer navigation is limited to working Home, Shop, Bag, and Account links, with CMS navigation filtered to valid targets. Deployed commit `f3ccd43`; production build and buyer route checks passed. Backup is beside `EditorialTemplate.tsx` with suffix `_nav-clean`.
- **ALURELAB Production QA (2026-09-18)**: Added Playwright browser QA template under `frontend/qa/` for buyer and seller smoke flows. Real production run found stale in-memory Next.js build: HTML references old static hashes returning HTTP 400 while server `.next/static` contains newer hashes. Buyer/seller JS flows are blocked until PM2 `alurelab-frontend` is restarted; restart not performed without explicit confirmation. Empty checkout guard passed. Evidence was captured in ignored `frontend/test-results/` and `frontend/qa-report/`.
- **ALURELAB QA Rerun (2026-09-18)**: PM2 `alurelab-frontend` restarted and static asset hashes now return 200. Buyer login/logout and empty-checkout guard pass. Real buyer add-to-bag flow still leaves cart empty, so checkout link is unavailable. All documented seller accounts return API 422 `Silakan verifikasi email Anda terlebih dahulu`; seller QA is blocked until a verified QA seller exists.
- **ALURELAB E2E Loop (2026-09-18)**: Buyer smoke now passes 3/3 in production after deploying a guard against clicking product actions before product data is loaded; build and PM2 restart passed. Backend storefront product list/detail now scopes queries by `tenant_id` for multi-store isolation, with production source backup suffix `_20260918_tenant-qa`. Full seller/order/shipping/finance mutation loop is blocked until a verified seller QA account and a store with valid Biteship `address_area_id` are available. Local PHPUnit is blocked by PostgreSQL not running on `127.0.0.1:5432`.
- **ALURELAB Semi-Production Loop (2026-09-18)**: Created isolated verified seller QA account/store `qa-loop-20260918` via Tinker after backing up target tables. Seller category, product+variant, buyer login, COD checkout/order, seller `cod_verified -> processing`, and finance page all passed. Shipment booking reaches Biteship but provider returns `40002002 Key has not been activated`, surfaced as HTTP 503; no fake shipment was created. Loop is blocked until Biteship API key is activated for booking.
- **ALURELAB Semi-Production Non-Shipping Loop (2026-09-18)**: With `QA_SKIP_SHIPPING=1`, the mutation loop passes category, product+variant, buyer login/cart/COD checkout, buyer order history, seller processing, and finance. Biteship shipment remains intentionally skipped until key activation.
- **ALURELAB QA Simulation Loop (2026-09-18)**: Added server-side QA-only simulation gated by `QA_SIMULATION_ENABLED` and exact `QA_SIMULATION_STORE_SLUG=qa-loop-20260918`. ONLINE payment becomes PAID with escrow ledger credit, logistics rates are local QA rates, and shipment creates QA AWB through the normal one-click order endpoint. Full semi-production loop with `QA_PAYMENT_METHOD=ONLINE` passes through shipped and finance. Other stores remain on real Xendit/Biteship.
- **ALURELAB 100-Store Load (2026-09-18)**: Created `qa-load-001` through `qa-load-100`, each with wallet, product, variant, and stock 1000. Public store/catalog load across 100 parallel workers returned 0 errors; max observed latency was 6.4s store and 8.3s catalog. 100 parallel simulated ONLINE checkout requests returned 100/100 HTTP 201, max 6.84s. Database verification: 100 QA stores, 200 orders, 200 `PAID_ESCROW`, 200 paid payments, 200 escrow transactions. Server observed load average 18-21 and swap nearly full; baseline was not captured, so capacity conclusion is pending proper monitoring.
- **ALURELAB CMS/Buyer Synchronization (2026-09-18)**: CMS now matches the simplified L Kids buyer surface. Removed stale collection/lookbook/trust toggles and About image/quote/value fields from the editor model, migrated legacy saved settings on load/save, locked navigation destinations to valid Home/Shop/Bag/Account routes, and removed custom-page menu injection. Deployed commits `ee33bdf` and `bbacb4a`; production build passed. Backups: CMS `page.tsx.bak_20260918_092730_cms-type-fix`, Editorial template `EditorialTemplate.tsx.bak_20260918_092611_cms-sync`, types `types.ts.bak_20260918_092611_cms-sync`.
- **ALURELAB Kopitiam CMS Test (2026-09-18)**: Production CMS settings for store slug `kopitiam` were changed for visual testing to coffee branding: `KOPITIAM`, `FRESHLY BREWED`, `SHOP COFFEE`, coffee hero/about copy, and valid four-item navigation. Legacy Kalmora CMS content and custom pages were removed from that store's settings; products were not changed and remain empty. Backup: `/home/alurelab/repositories/kopitiam_cms_before_test_20260918_093421.json`. Storefront `/kopitiam` returns 200 and verified new copy; rollback by restoring the JSON settings/name to the store record.
- **ALURELAB Five CMS Templates (2026-09-18)**: Added universal buyer CMS contract and five responsive templates: Editorial, Modern, Marketplace Pulse, Split Editorial, and Brutalist Signal. `buyerCopy` is editable in Dashboard CMS and consumed by product, cart, checkout, and account pages; public storefront settings now expose `highlights` and `buyerCopy`. Production backup: `/home/alurelab/app.alurelab.com.bak_20260918_5templates.tar.gz`. Remote build passed, PM2 `alurelab-frontend` restarted online, and all five `kalmora?template=...` routes returned HTTP 200. Local PHPUnit remained blocked by PostgreSQL not running on `127.0.0.1:5432`.
- **ALURELAB Buyer Theme Follow-up (2026-09-18)**: Internal buyer routes were still hardcoded to legacy layouts. Added shared theme frame/navbar across product, cart, checkout, account, order tracking, and custom pages; homepage now uses one shared navbar with Home, Shop, Cart, Checkout, and Account links. CMS navigation labels remain editable while destinations are locked to valid buyer routes. Production backup: `/home/alurelab/app.alurelab.com.bak_20260918_buyer-navbar.tar.gz`; remote build passed, PM2 online, and all buyer routes returned HTTP 200.
- **ALURELAB Shop/Theme Flash Fix (2026-09-18)**: Added dedicated `/{store_slug}/shop` catalog route with filters, search, product links, and add-to-cart. Moved CMS theme settings into server `BuyerThemeProvider` from storefront layout so internal pages render the correct theme immediately without a client-fetch flash. Production backup: `/home/alurelab/app.alurelab.com.bak_20260918_shop-route.tar.gz`; remote build passed, PM2 online, and shop/product/cart/checkout/account routes returned HTTP 200.
- **ALURELAB Kopitiam Product Seed (2026-09-18)**: Created exactly 50 active products for `kopitiam`, each with populated detail description, image, price, and one standard variant with stock 50. Verification: 50 products, 50 variants, 50 descriptions. All retained products are in `Coffee Beans`; backup before insert: `/home/alurelab/repositories/kopitiam_products_before_20260918_153314.json`; backup of intermediate 200-product state: `/home/alurelab/repositories/kopitiam_products_overgenerated_20260918_153314.json`.
- **ALURELAB Biteship Rates Verification (2026-09-18)**: `kopitiam` origin `ID_ID_3505_350509` and live Biteship key are configured. Production rates endpoint returned HTTP 200 with real JNE rates; checkout radio key/checked logic fixed to use `courier_code` and `courier_service_code`. Backup: `/home/alurelab/app.alurelab.com.bak_20260918_biteship-rates.tar.gz`.
- **ALURELAB Biteship Auto Rates (2026-09-18)**: Checkout now auto-loads Biteship rates when a logged-in buyer's saved address already has an `areaId`; manual region selection still loads rates. Production backup: `/home/alurelab/app.alurelab.com.bak_20260918_biteship-auto-rates.tar.gz`; remote build passed, PM2 online, rates endpoint returned HTTP 200.
- **ALURELAB Hydration Fix (2026-09-18)**: React error #418 (hydration mismatch) caused by `BuyerNavbar` reading zustand `persist` stores (cart count, buyer) that differ between SSR (defaults) and client (hydrated from localStorage). Fix: `useHydrated()` hook delays cart count/buyer render until client mount. Production backup: `/home/alurelab/app.alurelab.com.bak_20260918_hydration-fix.tar.gz`; remote build passed, PM2 online.
- **ALURELAB Checkout 500 Fix (2026-09-18)**: `POST /api/v1/checkout` returned 500 when Xendit secret key was missing — `XenditService::createInvoice()` threw `RuntimeException` inside `DB::transaction()`. Fix: added `XenditService::isConfigured()` public method + early validation in `StorefrontController::checkout()` before stock lock. Production now returns proper JSON error `PAYMENT_UNAVAILABLE` (503) instead of 500.
- **JOMOTO OCBC Token Fix (2026-09-22)**: MTI UAT token returned `4017300 Unauthorized` when the signed timestamp used UTC `+00:00`; Base64 RSA signature succeeded after timestamp changed to `now('Asia/Jakarta')->toIso8601String()` (`+07:00`). Deployed commit `6ea589fc` to production. Token runtime returned `2007300 Successful` with `expiresIn=900`; homepage and invalid-signature callback checks passed. Production backup: `/home/alurelab/jomotocenter.before_ocbc_timestamp_20260922_1555.tar.gz`. Real QR generation remains pending a safe QA order/payment; no customer order was used.
- **KBKB Revisions Deploy-ready (2026-09-23)**: 5 revisi diimplementasi & di-push ke `Gen-ei-Ryodan/kabekabe` commit `e45ff09`: (1) portal login terpisah `/login` member, `/partner` guard baru, `/admin`; (2) isolasi multi-guard `auth:partner` + logout regenerate; (3) kartu Paket Aktif `active_package` di home member; (4) popup approve partner + kait member opsional (`partners.member_user_id`, endpoint `GET admin/members/search`); (5) kode member `7030YYMMNNN` reset bulanan. Local: 152 tests / 927 assertions PASS, `npm run build` OK, migrasi lokal jalan (backup `database/database.sqlite.bak_20260923_before_member_user_id`). **Belum di-deploy ke server kbkb.id**: SSH `~/.ssh/id_ed25519_kbkb` passphrase-protected, agent kosong — user harus `ssh-add` dulu. SOP deploy: backup mysqldump `kbkbid_membership` + tar repo → push → `bash /home/kbkbid/deploy-kbkb.sh` → cek `/login` `/partner` `/admin` 200. Dokumentasi baru: `~/knowledge/10-Projects/KBKB/{README,HOWTO-HOSTING}.md`.
- **ALURELAB 16 Revisi Local (2026-09-24)**: 16 revisi frontend+backend selesai di local (belum commit/deploy): (1) landing nav/footer link `#features`/`#security` + halaman `/legal/terms` & `/legal/privacy` baru; (2) footer BuyerTheme Terms/Privacy pakai Link; (3) onboarding WA numeric-only + Eye toggle password; (4) `fetchApi` surface error validasi pertama (`data.errors`); (5) AuthController/ MerchantController pesan validasi email Indonesia; (6) login "Configuration" fix via `BackendSigninError extends CredentialsSignin` dengan `code` pesan backend + map di login page; (7) dashboard `main` `min-h-0` fix scroll; (8) MerchantController `getDashboardOverview` return `stats` real (GMV/orders/products) + `recent_orders` tenant-scoped; (9) settings kode pos rehydrate + 4 field wilayah wajib sebelum save + `WilayahAddressFields required` prop; (10) OrderController `createShipment` preflight 422 + BiteshipService error detail; orders page mapping service sicepat reg/best/siunt, jne reg/yes/oke, jnt ez; (11) analytics `gmv_growth_percent` real 7hari vs 7hari sebelumnya (null → "Belum ada pembanding"); (12) settings origin alamat prov/kab/kec/des wajib; (13) ProductController filter `action_required`(tanpa varian stok>0)/`review`(desc/images kosong) + `low_stock` via whereHas + products page badge count via Promise.all 2 query; (14) PDP/cart/cart-store stok 0: disable beli, cap qty, badge "Stok Habis", `stock` di CartItem; (15) checkout `resolvedAreaId=form.areaId||buildAreaId(wilayah)`, prefill dari kode wilayah bila areaId kosong, `ratesAttemptedFor` cegah loop; (16) default ongkir 17000→0, courier '' awal, row Ongkir '-' bila belum pilih, submit disabled tanpa courier/rates/stock issue. Verifikasi: `npx tsc --noEmit` clean, `npm run build` pass (semua route baru ada), `php -l` 5 file OK, `pint --dirty` pass. **Belum commit & belum deploy ke production.**
- **KBKB 10 Revisi Local (2026-09-24)**: 10 revisi selesai di local, di atas commit `e45ff09`, **belum commit & belum deploy**: (1) kartu Paket Aktif pindah `Member/Home` → `Member/Account/Billing` (`HomeController` tak lagi kirim `active_package`, `BillingController::index()` kirim `active_package` + `days_remaining`); (2) i18n lengkap — 461 key id/en paritas 0 selisih, file baru `dictionaries/{id,en}/partner.js`, 33 key `flow.*`, 13 `auth.success.*` + `auth.role.*`; (3) vendor promo punya `Detail` + `Edit` tiap row — route `vendor.promos.show` (`routes/web.php`), `VendorPromoController::show()`, page baru `Vendor/Promos/Show.jsx`, `PromoPolicy::update` di-relax jadi vendor-own status apa pun (update selalu `PromoService::submit()` → pending + notify admin), `Vendor/Promos/Edit.jsx` header jadi status-aware; (4) carousel foto (prev/next/dots/counter, tanpa library) di `Member/Promos/Show.jsx` + 7 key `promos.gallery.*`; (5) teks sukses registrasi `auth.success.*`; (6) `MAIL_FROM_ADDRESS="noreply@kbkb.id"` di `.env`/`.env.example`/`config/mail.php`; (7)(8)(9) OTP ganti password, email approval + password awal, OTP forget password — sudah ada & lolos test; (10) "flow flow flow" hilang karena key `flow.*` kini lengkap. Member pages ikut di-wire ke key: History, Notifications, Partners Index/Show, Promos Show, Agendas Show, Account Edit body. Verifikasi: `vendor/bin/phpunit` **153 tests / 936 assertions PASS**, `npm run build` PASS, audit i18n **MISSING: 0 DYNAMIC: 1** (`notifications.type.${type}`, dinamis by design), `pint --dirty` clean. Backup: `.backup/20260924_revisi10/` (33 file). Audit tooling: `/tmp/audit_i18n.mjs` (regex lookbehind, skip key ber-`${`).
- **GEMINI-WORKER CLI (2026-09-25)**: CLI baru `~/.antigravity/tools/gemini-worker/research.js` — terima prompt via argumen/`-p`/`-f`, kirim ke Gemini web memakai profil Brave yang di-copy ke temp, pantau streaming s/d stabil, simpan ke `-o` (backup `.bak_<ts>` otomatis). Wajib `ignoreDefaultArgs: ['--use-mock-keychain','--password-store=basic']` — tanpa itu Playwright menambahkan flag mock keychain sehingga semua cookie Google gagal decrypt (0 cookie) dan sesi dilaporkan padahal login. Deep Research (UI Bahasa Indonesia): `button[aria-label*="alat"]` (Upload & alat) → `Alat lainnya` → `Deep Research`; setelah submit muncul rencana riset dengan tombol **`Mulai penelitian`** (muncul belakangan, perlu grace window + klik dari loop polling). DOM Gemini: `response-container`, `user-query-content`, `pending-response`, `assistant-messages-primary`; deteksi login pakai zero-state `Meet Gemini, your personal AI assistant` (cookie `__Secure-1PSID` saja false-positive). Anti-gagal: auto-retry saat chat reset ke home, watchdog stall 10 menit, fallback tanpa Deep Research bila output <2500 char. **Session Google account dibatalkan Google di tengah otomasi Deep Research** (cookie masih ada tapi myaccount redirect ke halaman publik) → run berikutnya jalan guest/Flash-Lite tanpa Deep Research; perlu re-login Google di Brave untuk mode Deep Research & model Pro. Hasil riset ALURELAB: `PROJECTS/finance/PROJECT/PROJECT ANTIGRAVITY/ALURELAB/marketing-research-alurelab.md` (7.7k char, 4 bagian), backup11.4k versi Pro di `.bak_1790271801587`.
- **ALURELAB Content Batch 20 Post (2026-09-25)**: Batch konten naik dari 5-7 ke **20 post** (`ALURELAB/content-batch/post-01/` s/d `post-20/`, masing-masing `image.png` + `caption.md`). Generator: `~/.antigravity/tools/gemini-worker/content-generator.js`, flag `--posts 20 --until 04:45 --delay 30-60 --wait-research 1 [--dry-run]`; resume per-post (skip yang sudah lengkap), lock `/tmp/alurelab-content-generator.lock` mencegah 2 browser tabrakan, throttle terdeteksi → jeda 30-60s lalu retry maks 3x. **Caption 20/20 selesai** (02:04-02:26 WIB, total ~65k char, semua lolos cek format X/LinkedIn/IG + CTA + tanpa label `Gemini said`); prompt sudah di-hardening DILARANG mengarang klaim "klien kami"/angka hasil proyek (draft awal sempat mengarang "140 jam terpangkas untuk klien kami"). **image.png BUKAN Imagen 3**: session Google masih Signed-out (cookie `__Secure-1PSID` valid s/d 2027 tapi Google bilang "Signed out" di account chooser; sign-in otomatis ditolak "This browser or app may not be secure") → Gemini mode guest/Flash-Lite menolak bikin gambar. `isGuest()` di script otomatis skip Imagen, dan semua run memakai **fallback render lokal 1080x1080**. **Fix layout PNG (2026-09-25)**: profil Brave punya `devicePixelRatio = 0.8` → `setViewportSize(1080,1080)` menghasilkan `innerWidth = 1350` CSS px sehingga canvas terisi ~80% (teks kepotong); solusinya wrapper `.stage` 1080x1080 + `transform: scale(window.innerWidth/1080)` lalu screenshot viewport tanpa clip (`deviceScaleFactor` di launch options TIDAK meng-override DPR). **Sub text** dipotong di 160 char pada batas kata + `…` biar tidak terasa menggantung. Redraw final 20/20 image (07:34-07:40 WIB, `DONE ok=20 fail=0`, 20 md5 unik, caption tidak ikut diregenerate). Backup: `content-batch/image-batch-v1-before-redraw.tar.gz` (desain v1), `image-batch-v2-before-ellipsis.tar.gz` (v2 tanpa `…`), `content-generator.js.bak_20260925_073400_pre-ellipsis`. **Cara dapat Imagen asli**: login manual sekali di Brave → `rm post-*/image.png && node content-generator.js --posts 20`. **Tool `read` opencode sering menampilkan konten gambar SALAH** (cache/path silang): `post-16/image.png` pernah dibaca jadi desain post-01 padahal dua file identik byte (`md5 fe0e61c1...`); verifikasi mapping gambar WAJIB pakai `md5`/`cmp`, jangan percaya `read` sekali baca. Backup caption lama: `content-generator.js.bak_20260925_015844_pre-guestfix`, polluted caption di `caption.md.bak_*_polluted` / `*_karang-klaim`.
- **KBKB Partner Login Overlay (2026-09-25)**: Login Partner (`/partner`, `Auth/Login.jsx` branch `isPartner`) dirombak jadi **form overlay di dalam image** seperti member: kartu `aspect-[1024/1536] min-h-[700px] max-w-lg bg-black`, `<img partner-portrait.png>` absolut `object-cover object-top`, form absolut `top-[54%] bottom-5/bottom-8` + `justify-end` (teks PARTNER di 50.5% image → gap 34-54px ke label Email, tidak overlap), mobile **full-bleed** (`max-sm:-mx-4 max-sm:w-[calc(100%+2rem)]`) supaya crop samping tidak memotong huruf "PARTNER" (crop source = 512 − W/(2·scale); aman ≥ W 336px). **Landscape TIDAK dipakai**: aspect 2.24:1 → area hitam bawah hanya ~15% tinggi, form (~300px) tidak muat; desktop pakai portrait card (user izinkan opsi ini). Verifikasi: `npm run build` ✓, `vendor/bin/phpunit` 153/936 ✓, screenshot Playwright 320/360/390/414/480/640/1280 → tidak ada overlap & `scrollWidth == clientWidth` (tanpa scroll horizontal), console error 0; `/login` & `/admin` tetap 2 kartu (mobile+desktop), `/partner` 1 kartu. Backup: `resources/js/Pages/Auth/Login.jsx.bak_20260925_partner-overlay`. Catatan: commit eksternal `9d46ac0` (bukan dari sesi ini) sudah meng-commit isi file ini.
- **GEMINI-WORKER Resume Flag (2026-09-25)**: `research.js --resume` (alias `--resume-latest`) di `~/.antigravity/tools/gemini-worker/` — buka Gemini, buka sidebar (`button[aria-label="Buka sidebar"]`, sidebar default COLLAPSED), tarik history dari `a[href^="/app/<id>"]` (muncul di section "Terbaru"), urutkan kandidat relevan dulu (regex `alurelab|marketing|pemasaran|riset|research|pasar`), scan maks 8 chat: **jika chat sudah generate (`button[aria-label="Hentikan respons"]`) → ditonton TANPA klik**; jika ada tombol `Mulai penelitian` yang enabled → klik → verifikasi sukses = generating ATAU tombol hilang ATAU tombol jadi `disabled` (SETELAH diklik tombol tetap terlihat tapi `disabled` — bug verifikasi lama mengira klik gagal sehingga mengklik chat berikutnya). Output default `PROJECTS/finance/PROJECT/PROJECT ANTIGRAVITY/ALURELAB/marketing-research-alurelab.md` (backup `.bak_<ts>` otomatis); mode resume TIDAK pernah kirim ulang prompt (`noResend`), dan tidak boleh menyatakan selesai bila belum pernah terlihat generating + output <2500 char (teks rencana ≠ hasil riset). Tombol `Mulai penelitian` bisa `disabled` sementara saat chat masih load → tunggu s/d 12s sebelum putuskan skip. Verifikasi live: 30 chat terdeteksi, klik menghasilkan "Mulai meriset…" + stop button. **Insiden**: verifikasi bug pertama bikin 5 chat ALURELAB keklik & sama-sama jalan research server-side; sesudahnya **Google mencabut session** (cookie terdekripsi normal exp 2027 tapi shell Gemini "Sign in") → perlu re-login Brave lagi sebelum watch/simpan hasil bisa diverifikasi end-to-end. **Gotcha `read` opencode**: tool `read` pernah mengembalikan konten STALE research.js (429 baris versi lama) padahal disk sudah 484 baris — verifikasi file dengan `wc`/`grep`/`node --check`, jangan satu kali `read`. Backup: `research.js.bak_20260925_resume`. Ada 3 sesi `opencode run` concurrent di folder yang sama (task flow-fix, task resume, test research) — risiko saling timpa file.
- **GEMINI-WORKER Deep Research Flow Fix (2026-09-25)**: Alur Deep Research `research.js` diperbaiki sesuai alur asli Gemini Web (prompt → rencana 1-3 mnt → klik `Mulai penelitian` → riset 5-15 mnt → simpan). Perubahan: `START_SEL` diperluas (ID+EN: `Mulai penelitian`, `Mulai penelitian sekarang`, `Start research`, `Start researching`, + `aria-label`); `probeStartResearch()` deteksi tombol **visible vs enabled** (tombol `disabled` saat rencana belum selesai → tetap tunggu, jangan diklik); deteksi rencana via `PLAN_RE` (`Rencana penelitian/riset`, `Research plan`, `Menyusun rencana`, ...) + fallback `!generating && raw>150`. **Toleransi tombol 5 menit** (`PLAN_WAIT_MS`, log per menit `Masih menunggu tombol "Mulai penelitian"... n/5 menit`); lewat 5 menit → resend (maks 2x), dan kalau retry habis **tetap menunggu sampai timeout global, tidak break premature**. Polling `POLL_MS` 3000→2000ms dan blok `!started` dipindah SEBELUM cek stall. **Stall 10→15 menit + syarat `!generating`** — ini penting: teks riset bisa membeku >15 menit dengan stop-button masih ada (panel riset), tanpa guard itu script kirim ulang di tengah riset (terbukti di test run pertama, `research.js` versi pre-guard resend di menit ke-15). Timeout `--timeout` di-clip minimum **30 menit**. Deteksi login: zero-state yang kebetulan muncul sesaat dengan cookie valid → recheck sekali (+5s) sebelum memaksa mode guest/non-deep (false-negative ini pernah meloloskan run jadi non-deep). Fase log wajib: `Menunggu rencana riset...` → `Rencana terdeteksi -> menunggu tombol "Mulai penelitian" (toleransi 5 menit)` → `Rencana terdeteksi, mengklik Mulai penelitian...` → `Sedang riset mendalam di web (estimasi 5-10 menit)...`. **Verifikasi end-to-end live**: run test kedua `STATUS: OK`, 29002 char, plan 4 dtk setelah submit, klik 09:42:29, selesai 10:01:19 (riset ~19 mnt) tanpa resend. Test pertama (kode pre-guard) kena stall-resend → `STATUS: TIMEOUT_PARTIAL` 5745 char. Backup: `research.js.bak_20260925_090526` (versi sebelum fix). Catatan: sesi `--resume` menulis file yang SAMA di 09:29 — kedua fitur tergabung bersih, `node --check` OK.
- **KBKB Partner Login Margin (2026-09-25)**: Full-bleed mobile partner card dihapus (`max-sm:-mx-4 max-sm:w-[calc(100%+2rem)]` → `max-sm:mx-auto max-sm:max-w-[390px]`) di `Auth/Login.jsx:148`, sehingga gap 16px (px-4 container) kiri-kanan kartu portrait di HP, frame emas tidak mepet tepi layar. Verifikasi: `npm run build` ✓, `vendor/bin/phpunit` 153/936 ✓, screenshot Playwright 390/360/320 → card x=16 gap 16px, form di dalam kartu (bottom 775 ≤ 779), no hscroll; 390px "PARTNER" utuh, 320px sudah terpotong (pre-existing, < ambang 336px, sama seperti full-bleed). Backup: `resources/js/Pages/Auth/Login.jsx.bak_20260925_partner-margin`.
